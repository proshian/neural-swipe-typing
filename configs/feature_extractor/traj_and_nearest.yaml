# Trajectory + nearest key feature extraction
- type: trajectory
  params:
    include_dt: false
    include_velocities: true
    include_accelerations: true

- type: nearest_key
  params:
    use_lookup: true
