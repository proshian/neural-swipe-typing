# Google 2015 feature extraction (dt + nearest_key)
- type: trajectory
  params:
    include_dt: true
    include_velocities: false
    include_accelerations: false

- type: nearest_key
  params:
    use_lookup: true
