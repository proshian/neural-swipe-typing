# @package _global_

defaults:
  - /train
  - override /encoder: transformer_v1
  - override /decoder: transformer_v1
  - override /swipe_point_embedder: separate_traj_and_nearest__4_coord
  - override /feature_extractor: indiswipe
  - _self_
