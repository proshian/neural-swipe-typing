# @package _global_

defaults:
  - /train
  - override /encoder: transformer_v1
  - override /decoder: transformer_v1
  - override /swipe_point_embedder: separate_traj_and_trainable_weighted__6_coord
  - override /feature_extractor: traj
  - _self_
